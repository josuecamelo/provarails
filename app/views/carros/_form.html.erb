<%= form_with(model: carro, local: true) do |form| %>
  <% if carro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carro.errors.count, "error") %> prohibited this carro from being saved:</h2>

      <ul>
      <% carro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 

  <%= form.label :marca %>
  <%= form.text_field :marca %>

  <%= form.label :modelo %>
  <%= form.text_field :modelo %>

  <%= form.label :disponivel %>
  <%= form.select :disponivel, [['Não', false], ['Sim', true]] %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>