<p id="notice"><%= notice %></p>

<h1>Reservas</h1>

<table>
  <thead>
    <th>#</th>
    <th>Responsável Reserva</th>
    <th>Marca</th>
    <th>Modelo</th>
    <th>Data Reserva</th>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reservas.each do |reserva| %>
      <tr>
        <td><%= reserva.id %></td>
         <td><%= reserva.responsavel %></td>
        <td><%= reserva.carro.marca %></td>
        <td><%= reserva.carro.modelo %></td>
        <td><%= reserva.created_at.strftime("%d/%m/%Y") %></td>
        <td><%= link_to 'Exibir', reserva %></td>
        <td><%= link_to 'Editar', edit_reserva_path(reserva) %></td>
        <td><%= link_to 'Excluir', reserva, method: :delete, data: { confirm: 'Excluir Reserva?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Reserva', new_reserva_path %>
